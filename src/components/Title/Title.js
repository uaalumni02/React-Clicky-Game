import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Supercar Clicky Game!</h1>
        <p>Click on an image to earn points, but you cannot click the same image more than once!</p>
    </div>
);

export default Title;

